const NewBugPanel = (props) =>{

    const classes = props.className;

    return(
        <div className={classes}>
            <h1>New Bug Page</h1>
        </div>
    )

}

export default NewBugPanel;