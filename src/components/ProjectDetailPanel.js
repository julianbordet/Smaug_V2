const ProjectDetailPanel = (props) =>{

    const classes = props.className;

    return(
        <div className={classes}>
            <h1>Project detail panel</h1>
        </div>
    )

}

export default ProjectDetailPanel;